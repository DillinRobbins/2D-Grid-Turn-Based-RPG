using Newtonsoft.Json;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using UnityEngine;

public static class SaveSystem
{
    /*static Dictionary<string, object> instances = new();


    public static T LoadFile<T>()
    {
        string savePath = "pretend path.txt";
        string json = File.ReadAllText(savePath);
        T myLoadedObject = JsonConvert.DeserializeObject<T>(json);
        return myLoadedObject;
    }

    public static void Save<T>(T objectToSave, string path)
    {

    }*/
}
